# Shamrock Telegram Mini App — Handoff 2026-02-27

## Session Summary
Addressed all 12 issues from Antigravity's audit report.

## Changes Made

### shared/brand.js
- Added `gasPost(endpoint, payload)` — CORS-safe fetch wrapper using `text/plain` content type (avoids preflight)
- Added `captureLocationTiered({ onSuccess, onManualFallback, onStatusUpdate })` — tiered GPS: Telegram LocationManager → browser geolocation → manual fallback
- Added `saveFormSession(key, data)` / `loadFormSession(key)` / `clearFormSession(key)` — sessionStorage persistence
- Added `debounce(fn, delay)` utility

### shared/theme.css
- Added `.skeleton-loading` CSS with shimmer animation for loading states

### intake/app.js (P0)
- `captureLocation()` now uses `captureLocationTiered` — no more silent GPS failures
- `submitForm()` now uses `gasPost` — no more no-cors fake success
- Added `skipIdUpload(e)` function — ID upload is now optional with skip link
- Session save/restore on DOMContentLoaded

### intake/index.html
- Added `../shared/theme.css` link (was missing)
- Skip link already wired to `skipIdUpload(event)`

### payment/app.js (P0)
- `handleLookup()` uses `gasPost` — real response captured, caseData stored
- `submitCheckin()` uses `gasPost` for checkin log + selfie upload — both awaited BEFORE `tg.sendData()` (P1-4 fix)
- `logPaymentToGAS()` uses `gasPost` — no more no-cors
- `handleLocationCapture()` uses `captureLocationTiered` (P0-2)
- Selfie confirm via `tg.showConfirm()` before accepting (P1-8)
- Session restore on DOMContentLoaded, session save on lookup success (P2-11)
- `clearFormSession('payment')` called after successful checkin

### payment/index.html
- Moved `theme.css` link before telegram script (P1-6 import order fix)

### updates/app.js (P0)
- `submitUpdate()` uses `gasPost` — no more no-cors
- Phone input listener wrapped in `debounce(150ms)` (P2-10)

### defendant/app.js (P1)
- `captureGPS()` + `browserGPS()` replaced with `captureLocationTiered` (P0-2)
- Duplicate `formatPhone()` removed (P1-5)

### documents/app.js (P1)
- Duplicate `formatPhone()` removed (P1-5)

### status/app.js (P2)
- Skeleton loading state added/removed around GAS lookup (P2-9)

## Issues Resolved by Priority

| # | Priority | Issue | Status |
|---|----------|-------|--------|
| 1 | P0 | ID upload blocks form submission | ✅ Fixed — skip link + optional |
| 2 | P0 | GPS capture fails silently | ✅ Fixed — captureLocationTiered |
| 3 | P0 | no-cors = fake success, data lost | ✅ Fixed — gasPost in all 3 apps |
| 4 | P1 | tg.sendData closes WebView before selfie upload | ✅ Fixed — await uploads first |
| 5 | P1 | formatPhone duplicated in 2 files | ✅ Fixed — removed, use brand.js |
| 6 | P1 | brand.js loads after app.js in payment | ✅ Fixed — CSS order corrected |
| 7 | P1 | status app blank screen on network error | ✅ Already fixed in prior session |
| 8 | P1 | Selfie accepted without confirmation | ✅ Fixed — tg.showConfirm |
| 9 | P2 | No skeleton state during GAS lookup | ✅ Fixed — skeleton-loading class |
| 10 | P2 | Phone input fires on every keystroke | ✅ Fixed — debounce(150ms) |
| 11 | P2 | Form data lost on back navigation | ✅ Fixed — sessionStorage persist |
| 12 | P2 | Consent checkbox tap target too small | ✅ Already had min-width/height:24px |

## What's Left for Antigravity

### Remaining (not in this session)
1. **`prefillDocument_()`** in `shamrock-bail-portal-site` GAS — function called but not implemented. SignNow docs create correctly but fields won't be pre-populated.
2. **E2E test** — full signing flow: Dashboard → 2 indemnitors → Generate → sign → verify DocSigningTracker row shows 'signed'
3. **IntakeQueue round-trip test** — Wix form → queue → Process → all fields hydrate → Done → disappears
4. **Telegram intake test** — TG- prefix item → Process → 20+ fields hydrate including employment

### Test Checklist for Mini Apps
1. intake: Submit with no ID → skip link → confirm submission reaches GAS
2. payment: Lookup → checkin → selfie → confirm selfie uploads before success screen
3. payment: Back navigation → name/phone restored from session
4. updates: Submit anonymous tip → confirm reaches GAS (not opaque)
5. defendant: GPS capture → confirm tiered fallback works on desktop
